# Generador de IC Dinámica

## Instalación:

```
$ npm i
```

## Uso:

Completar la información de ./src/data.js, agregar las imágenes en la carpeta ./src/images y cambiar los colores en .src/css/index.scss

Luego ejecutar `$ npm run build` para generar los archivos.
